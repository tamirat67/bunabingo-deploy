const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const game = await prisma.game.findFirst({ where: { status: 'COUNTDOWN', room: { type: 'STANDARD' } }, include: { tickets: true } }); if (game) { const botUsers = await prisma.user.findMany({ where: { isBot: true, id: { in: game.tickets.map(t => t.userId) } } }); console.log('Total tickets:', game.tickets.length); console.log('Bot users:', botUsers.length); console.log('Real users:', game.tickets.length - botUsers.length); } else { console.log('No countdown game found'); } } run().finally(() => process.exit(0));
